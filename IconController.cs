using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IconController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // for pick up set up i.e able to pick up sword and rifle.
      // icon groups in hietachy to set active or false. basically play your melee and shooter images in group
      // set active false here then - pick script set true
      //note there is quite alot to set up for images and equipped items keep simple best to have a solid
      // - sytem than comlex not working sytem.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
